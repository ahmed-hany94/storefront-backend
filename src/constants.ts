import dotenv from 'dotenv';

dotenv.config();

const {
  POSTGRES_HOST,
  POSTGRES_USER,
  POSTGRES_PASSWORD,
  POSTGRES_DB_NAME,
  POSTGRES_TEST_DB_NAME,
  PORT,
  DB_PORT,
  TEST_DB_PORT,
  ENV,
  SALT_ROUNDS,
  JWT_SECRET,
  BCRYPT_SECRET
} = process.env;

export {
  POSTGRES_HOST,
  POSTGRES_USER,
  POSTGRES_PASSWORD,
  POSTGRES_DB_NAME,
  POSTGRES_TEST_DB_NAME,
  PORT,
  DB_PORT,
  TEST_DB_PORT,
  ENV,
  SALT_ROUNDS,
  JWT_SECRET,
  BCRYPT_SECRET
};
